{
  "name": "memtree",
  "version": "0.1.0",
  "description": "CLI for the memtree context store — visualize, inspect, and manage your AI session graph",
  "type": "module",
  "bin": {
    "memtree": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "mkdir -p dist && bun build src/cli.ts --target bun --outfile dist/cli.js && cp ../../mcp/src/visualize/ui.html dist/ui.html",
    "prepublishOnly": "bun run build"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "keywords": ["memtree", "visualizer", "graph", "sqlite", "claude", "mcp", "context"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/joeblackwaslike/memtree"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0"
  }
}
